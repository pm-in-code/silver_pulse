# Добавление файлов разделения WebView в Xcode

## Проблема решена:
WebView теперь разделен на два отдельных компонента для предотвращения перерисовки и обеспечения стабильной работы.

## Необходимо добавить следующие файлы в проект Xcode:

### 1. CoachWebViewOnly.swift
- **Путь**: `SilverPulse/Views/Call/CoachWebViewOnly.swift`
- **Описание**: Чистый WebView без таймера и других обновляемых элементов

### 2. CallUIOverlay.swift
- **Путь**: `SilverPulse/Views/Call/CallUIOverlay.swift`
- **Описание**: UI элементы (таймер, кнопки) без WebView

### 3. CoachCallView.swift
- **Путь**: `SilverPulse/Views/Call/CoachCallView.swift`
- **Описание**: Главный View, объединяющий WebView и UI Overlay

## Инструкции по добавлению:

### Добавление CoachWebViewOnly.swift:
1. Откройте Xcode проект `SilverPulse.xcodeproj`
2. В Project Navigator найдите папку `Views/Call`
3. Правый клик на папке `Call` → "Add Files to SilverPulse"
4. Выберите файл `CoachWebViewOnly.swift`
5. Убедитесь что "Add to target: SilverPulse" отмечен ✅
6. Нажмите "Add"

### Добавление CallUIOverlay.swift:
1. Правый клик на папке `Call` → "Add Files to SilverPulse"
2. Выберите файл `CallUIOverlay.swift`
3. Убедитесь что "Add to target: SilverPulse" отмечен ✅
4. Нажмите "Add"

### Добавление CoachCallView.swift:
1. Правый клик на папке `Call` → "Add Files to SilverPulse"
2. Выберите файл `CoachCallView.swift`
3. Убедитесь что "Add to target: SilverPulse" отмечен ✅
4. Нажмите "Add"

## Что изменилось:

### ✅ **Проблема решена:**
- **WebView стабилен**: Больше нет постоянных обновлений таймера
- **Разделение ответственности**: WebView отделен от UI элементов
- **Производительность**: Нет перерисовки WebView при обновлении таймера

### 🏗️ **Архитектура:**
1. **CoachWebViewOnly**: Только WebView, без обновляемых элементов
2. **CallUIOverlay**: Таймер, кнопки, алерты - все UI элементы
3. **CoachCallView**: Объединяет WebView и UI Overlay в ZStack

### 🎯 **Преимущества:**
- **Стабильный WebView**: Не перерисовывается при обновлении таймера
- **Чистая архитектура**: Разделение UI и WebView логики
- **Лучшая производительность**: Минимальные обновления
- **Простота отладки**: Каждый компонент отвечает за свою область

## Проверка:
После добавления файлов:
1. Проект должен собираться без ошибок
2. WebView должен работать стабильно без перерисовки
3. Таймер должен обновляться только в UI Overlay
4. Кнопки должны работать с вибрацией и звуками

## Функции:
- ✅ **Стабильный WebView**: Без постоянных обновлений
- ✅ **Рабочий таймер**: Обновляется только в UI слое
- ✅ **Все кнопки работают**: С вибрацией и звуками
- ✅ **Фоновая музыка**: Останавливается/возобновляется корректно
