# Настройка Entitlements в Xcode

## 🔧 **Проблема:**
- Ошибки с entitlements для WebKit Media Playback
- Аудио работает нестабильно
- Коуч не слышит пользователя

## ✅ **Исправления применены:**

### 1. **Упрощен файл entitlements**
- Убраны дублирующиеся ключи
- Оставлены только необходимые разрешения

### 2. **Улучшены настройки аудио**
- Убрана опция `.mixWithOthers` которая может мешать
- Добавлен JavaScript для принудительного включения аудио

### 3. **Добавлено JavaScript для аудио**
- Принудительное включение аудио элементов
- Логирование всех кнопок на странице
- Автоматическое воспроизведение

## 🚀 **Как настроить entitlements в Xcode:**

### 1. **Добавьте файл entitlements в проект**
1. **Откройте Xcode**
2. **Перетащите файл `SilverPulse1.1.entitlements`** в проект
3. **Убедитесь, что он добавлен в target "SilverPulse1.1"**

### 2. **Настройте Build Settings**
1. **Выберите проект** (самый верхний элемент)
2. **Выберите target "SilverPulse1.1"**
3. **Перейдите в "Build Settings"**
4. **Найдите "Code Signing Entitlements"**
5. **Установите значение:**
   ```
   SilverPulse1.1/SilverPulse1.1.entitlements
   ```

### 3. **Проверьте Info.plist**
Убедитесь, что в Info.plist есть:
```xml
<key>NSMicrophoneUsageDescription</key>
<string>This app needs microphone access for coach calls</string>
<key>NSCameraUsageDescription</key>
<string>This app needs camera access for coach calls</string>
<key>UIBackgroundModes</key>
<array>
    <string>audio</string>
    <string>voip</string>
</array>
```

## 🧪 **Тестирование:**

### 1. **Очистите и пересоберите**
```bash
# В Xcode:
Product → Clean Build Folder (Cmd+Shift+K)
Product → Build (Cmd+B)
Product → Run (Cmd+R)
```

### 2. **Проверьте аудио**
1. Нажмите "CALL" в лобби
2. **Проверьте консоль** - должны появиться:
   - "Audio session configured successfully"
   - "DOM loaded, enabling audio"
   - "Button X: [текст кнопки]"

### 3. **Проверьте звук**
1. **Должен быть слышен голос коуча**
2. **Попробуйте говорить** - коуч должен отвечать
3. **Проверьте кнопки** - должны работать

## 📱 **Ожидаемое поведение:**

- ✅ **WebView не моргает**
- ✅ **Слышен голос коуча**
- ✅ **Можно говорить с коучем**
- ✅ **Нет ошибок с entitlements**

## 🔍 **Если проблемы остаются:**

### **Проблема 1: Нет звука**
**Решение:** 
- Проверьте громкость устройства
- Проверьте разрешения микрофона
- Перезапустите приложение

### **Проблема 2: Ошибки entitlements**
**Решение:**
- Убедитесь, что файл entitlements добавлен в проект
- Проверьте путь в Build Settings
- Очистите DerivedData

### **Проблема 3: Коуч не слышит**
**Решение:**
- Проверьте разрешения микрофона
- Убедитесь, что кнопка микрофона работает
- Проверьте настройки аудио сессии

## 🚨 **Важно:**

После настройки entitlements:
- ✅ Приложение должно запросить разрешения
- ✅ Звук должен работать стабильно
- ✅ Микрофон должен работать
- ✅ Нет ошибок с entitlements

## 📊 **Логи для проверки:**

В консоли должны появиться:
- "Audio session configured successfully"
- "DOM loaded, enabling audio"
- "Button X: [текст кнопки]"
- "Button clicked: [текст кнопки]"
