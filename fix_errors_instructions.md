# Инструкции по исправлению ошибок

## ✅ Исправленные проблемы:

### 1. **"Publishing changes from background threads"**
- **Проблема**: Обновления UI происходили в фоновом потоке
- **Решение**: Добавлен `DispatchQueue.main.async` для всех обновлений UI
- **Файлы**: `QuotaService.swift`

### 2. **Неправильная логика таймера**
- **Проблема**: Таймер работал постоянно, даже когда пользователь не разговаривал
- **Решение**: Таймер теперь работает только во время активного разговора
- **Файлы**: `QuotaViewModel.swift`, `CoachWebView.swift`

### 3. **WebView ошибки**
- **Проблема**: WebView не мог загрузить контент коуча
- **Решение**: Улучшена конфигурация WebView и добавлена обработка ошибок
- **Файлы**: `CoachWebView.swift`

## 🚀 Как протестировать исправления:

### 1. **Очистите проект**
```bash
# В Xcode:
Product → Clean Build Folder (Cmd+Shift+K)
```

### 2. **Пересоберите проект**
```bash
# В Xcode:
Product → Build (Cmd+B)
Product → Run (Cmd+R)
```

### 3. **Тестирование таймера**
1. Запустите приложение
2. Пройдите онбординг
3. В лобби проверьте, что таймер показывает 4:00:00
4. **ВАЖНО**: Таймер НЕ должен отсчитывать в лобби
5. Нажмите "CALL" - таймер должен начать отсчитывать
6. Завершите разговор - таймер должен остановиться

### 4. **Тестирование WebView**
1. Нажмите "CALL" в лобби
2. WebView должен загрузить страницу коуча
3. Проверьте консоль на наличие ошибок загрузки
4. Попробуйте взаимодействовать с коучем

### 5. **Проверка логов**
В консоли Xcode не должно быть:
- ❌ "Publishing changes from background threads"
- ❌ "Failed to stop call: invalidEndpoint"
- ❌ WebView ошибки загрузки

## 📱 Ожидаемое поведение:

### **Таймер:**
- ✅ В лобби: показывает 4:00:00 (не отсчитывает)
- ✅ Во время разговора: отсчитывает время
- ✅ После разговора: останавливается на текущем значении

### **WebView:**
- ✅ Загружает страницу коуча
- ✅ Показывает интерфейс для разговора
- ✅ Нет черного экрана

### **Логи:**
- ✅ Нет ошибок background threads
- ✅ Нет ошибок WebView
- ✅ Нет ошибок call session

## 🔧 Если проблемы остаются:

1. **Перезапустите симулятор**
2. **Удалите приложение с симулятора**
3. **Пересоберите проект заново**
4. **Проверьте интернет-соединение** (для загрузки WebView)
